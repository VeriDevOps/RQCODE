plugins {
  id "org.sonarqube" version "3.3"
}

sonarqube {
  properties {
    property "sonar.projectKey", "VeriDevOps_RQCODE"
    property "sonar.organization", "veridevops"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
